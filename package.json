{
  "name": "soulgood",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/launch",
    "build": "npm run build --workspace apps/launch",
    "start": "npm run start --workspace apps/launch",
    "lint": "npm run lint --workspace apps/launch",
    "test": "npm run test --workspace apps/launch"
  }
}
